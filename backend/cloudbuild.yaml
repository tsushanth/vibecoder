steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/vibecoder/vibecoder-api:${SHORT_SHA}'
      - '-t'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/vibecoder/vibecoder-api:latest'
      - '.'
    dir: backend

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/vibecoder/vibecoder-api'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'vibecoder-api'
      - '--image'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/vibecoder/vibecoder-api:${SHORT_SHA}'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--update-secrets'
      - 'SUPABASE_URL=VIBECODER_SUPABASE_URL:latest,SUPABASE_ANON_KEY=VIBECODER_SUPABASE_ANON_KEY:latest,WORKER_SECRET=VIBECODER_WORKER_SECRET:latest'

images:
  - 'us-central1-docker.pkg.dev/${PROJECT_ID}/vibecoder/vibecoder-api'

options:
  logging: CLOUD_LOGGING_ONLY
